# Local development stack.
#
# Only the application services publish ports. PostgreSQL and Redis stay on the
# internal compose network - nothing outside the stack needs to reach them.

x-django-service: &django-service
  restart: unless-stopped
  env_file:
    - .env
  depends_on:
    postgres:
      condition: service_healthy
    redis:
      condition: service_healthy
  # The slim Python image ships no curl, so the probe uses the interpreter that
  # is already there. It hits the service's own /healthz/ on the internal port.
  healthcheck:
    test:
      - CMD-SHELL
      - >-
        python -c "import urllib.request,sys;
        sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/healthz/',
        timeout=5).status == 200 else 1)"
    interval: 15s
    timeout: 5s
    retries: 5
    start_period: 30s

services:
  postgres:
    image: postgres:16-alpine
    container_name: task_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d postgres"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 10s
  redis:
    image: redis:7-alpine
    container_name: task_redis
    restart: unless-stopped
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  auth-service:
    <<: *django-service
    build:
      context: ./services/auth-service
    container_name: auth_service
    # Source is mounted for local development so `runserver` reloads on edit.
    # Production images are self-contained - nothing is mounted there.
    volumes:
      - ./services/auth-service:/app
    environment:
      POSTGRES_DB: ${AUTH_DB_NAME}
    ports:
      - "8001:8000"

  engagement-service:
    <<: *django-service
    build:
      context: ./services/engagement-service
    container_name: engagement_service
    # Source is mounted for local development so `runserver` reloads on edit.
    # Production images are self-contained - nothing is mounted there.
    volumes:
      - ./services/engagement-service:/app
    environment:
      POSTGRES_DB: ${ENGAGEMENT_DB_NAME}
    ports:
      - "8002:8000"

  task-service:
    <<: *django-service
    build:
      context: ./services/task-service
    container_name: task_service
    # Source is mounted for local development so `runserver` reloads on edit.
    # Production images are self-contained - nothing is mounted there.
    volumes:
      - ./services/task-service:/app
    environment:
      POSTGRES_DB: ${TASK_DB_NAME}
    ports:
      - "8003:8000"

  worker-service:
    build:
      context: ./services/worker-service
    container_name: worker_service
    volumes:
      - ./services/worker-service:/app
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
      engagement-service:
        condition: service_started
      task-service:
        condition: service_started
    command:
      - celery
      - -A
      - app.celery:app
      - worker
      - --loglevel=info
      - -Q
      - engagement-events

  # Production image: the bundle is built at image-build time (Vite inlines
  # frontend/.env.production) and served by nginx. docker-compose.override.yml
  # swaps this for the Vite dev server on 5173 during local development.
  frontend:
    build:
      context: ./frontend
    container_name: frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - auth-service
      - engagement-service
      - task-service

volumes:
  postgres_data:
  redis_data:
